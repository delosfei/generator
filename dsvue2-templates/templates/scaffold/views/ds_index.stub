<template>
    <hd-tabs />
    <hd-table-list name="$MODEL_NAME_SNAKE$" @del="del" class="mt-3" />
</template>

<script setup>
import helper from 'utils/helper'
import $MODEL_NAME_SNAKE$Api from 'api/$MODEL_NAME_SNAKE$Api'

const del = async ({ row, query }) => {
    try {
        await helper.confirm(`确定删除【${row.title}】吗？`)
        await $MODEL_NAME_SNAKE$Api.destroy(row.id)
        query();
    } catch (e) {
        console.log(e)
    }
}
</script>

<style>
</style>